export const selectUserState = state => state.userData.user;

export const selectUserLoadingState = state => state.userData.loading;

export const selectUserSearchState = state => state.userData.search;
